const Airport = require('./airport');
//beforeAll(() => {
  //airport = new Airport;
//});
test('has no planes by default', () => {
  airport = new Airport;
  weather = new
expect(airport.planes()).toEqual([]);
});
